import Link from "next/link";import {Activity,BookOpen,ChartNoAxesCombined,Radio,Trophy} from "lucide-react";
export function Navbar({live=false}:{live?:boolean}){return <header className="sticky top-0 z-50 border-b border-white/[.07] bg-ink/80 backdrop-blur-xl"><div className="mx-auto flex h-16 max-w-[1440px] items-center justify-between px-5"><Link href="/" className="flex items-center gap-3 font-black tracking-tight"><span className="grid h-8 w-8 place-items-center rounded-lg bg-lime text-ink">TX</span>TxArena</Link><nav className="flex items-center gap-1 text-sm text-slate-400"><Nav href="/arena" icon={<Radio size={15}/>} label="Arena"/><Nav href="/leaderboard" icon={<Trophy size={15}/>} label="Leaderboard"/><Nav href="/logs" icon={<Activity size={15}/>} label="Logs"/><Nav href="/about" icon={<BookOpen size={15}/>} label="About"/></nav><span className="hidden items-center gap-2 rounded-full border border-lime/20 bg-lime/5 px-3 py-1.5 text-[10px] font-bold uppercase tracking-widest text-lime md:flex"><i className="live-dot"/>{live?"Live TxLINE Mode":"Demo Replay Mode"}</span></div></header>}
function Nav({href,icon,label}:{href:string;icon:React.ReactNode;label:string}){return <Link className="flex items-center gap-2 rounded-lg px-3 py-2 hover:bg-white/5 hover:text-white" href={href}>{icon}<span className="hidden sm:block">{label}</span></Link>}
export function Footer(){return <footer className="mt-20 border-t border-white/[.07] px-5 py-8 text-center text-xs text-slate-500">TxArena is a market-intelligence and strategy-simulation tool. It does not place bets or provide betting advice.</footer>}
export function PageHeader({eyebrow,title,subtitle,actions}:{eyebrow?:string;title:string;subtitle:string;actions?:React.ReactNode}){return <div className="mb-8 flex flex-col justify-between gap-5 lg:flex-row lg:items-end"><div>{eyebrow&&<p className="eyebrow mb-3">{eyebrow}</p>}<h1 className="text-3xl font-black tracking-tight md:text-4xl">{title}</h1><p className="mt-2 max-w-2xl text-sm text-slate-400">{subtitle}</p></div>{actions&&<div className="flex flex-wrap gap-2">{actions}</div>}</div>}
export function GlassCard({children,className=""}:{children:React.ReactNode;className?:string}){return <div className={`glass rounded-2xl ${className}`}>{children}</div>}
export function StatusBadge({children,tone="green"}:{children:React.ReactNode;tone?:"green"|"purple"|"blue"|"amber"}){const c={green:"border-lime/20 bg-lime/10 text-lime",purple:"border-violet/20 bg-violet/10 text-violet-300",blue:"border-cyan/20 bg-cyan/10 text-cyan",amber:"border-amber-400/20 bg-amber-400/10 text-amber-300"}[tone];return <span className={`inline-flex rounded-full border px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider ${c}`}>{children}</span>}
export function MetricCard({label,value,detail}:{label:string;value:string|number;detail:string}){return <GlassCard className="p-5"><p className="text-[10px] font-bold uppercase tracking-[.15em] text-slate-500">{label}</p><p className="mt-2 text-3xl font-black">{value}</p><p className="mt-1 text-xs text-slate-500">{detail}</p></GlassCard>}
