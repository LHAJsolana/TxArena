"use client";import {useEffect,useState} from "react";import {GlassCard,PageHeader,StatusBadge} from "./shell";type S={id:string;score:number;accuracy:number;signalsGenerated:number;confidenceCalibration:number;reactionSpeed:number;consistency:number;volatilityAwareness:number;updatedAt:string;agent:{name:string;strategyType:string}};export function LeaderboardClient(){const[data,setData]=useState<S[]>([]);useEffect(()=>{const load=()=>fetch("/api/leaderboard").then(r=>r.json()).then(j=>setData(j.data));load();const i=setInterval(load,5000);return()=>clearInterval(i)},[]);return <div className="mx-auto max-w-[1200px] px-5 py-10"><PageHeader eyebrow="Transparent evaluation" title="Agent Leaderboard" subtitle="One weighted score makes autonomous strategy quality easy to compare and explain."/><div className="grid gap-4 md:grid-cols-3">{data.slice(0,3).map((s,i)=><GlassCard className={`p-6 ${i===0?"border-lime/20":""}`} key={s.id}><span className="font-mono text-xs text-lime">RANK 0{i+1}</span><h2 className="mt-5 text-xl font-black">{s.agent.name}</h2><p className="text-xs text-slate-500">{s.agent.strategyType}</p><p className="mt-7 font-mono text-4xl font-black">{s.score.toFixed(1)}</p><p className="text-[10px] uppercase tracking-widest text-slate-600">Arena score</p></GlassCard>)}</div><GlassCard className="mt-5 overflow-x-auto p-4"><table><thead><tr><th>Rank</th><th>Agent</th><th>Arena score</th><th>Accuracy</th><th>Signals</th><th>Calibration</th><th>Reaction</th><th>Consistency</th><th>Volatility</th><th>Last active</th></tr></thead><tbody>{data.map((s,i)=><tr key={s.id}><td className="font-mono">0{i+1}</td><td className="whitespace-nowrap"><b>{s.agent.name}</b><p className="text-[10px] text-slate-500">{s.agent.strategyType}</p></td><td className="font-mono text-lime">{s.score.toFixed(1)}</td><td>{s.accuracy.toFixed(1)}%</td><td>{s.signalsGenerated}</td><td>{s.confidenceCalibration.toFixed(1)}</td><td>{s.reactionSpeed.toFixed(1)}</td><td>{s.consistency.toFixed(1)}</td><td>{s.volatilityAwareness.toFixed(1)}</td><td className="whitespace-nowrap text-slate-500">{new Date(s.updatedAt).toLocaleTimeString()}</td></tr>)}</tbody></table></GlassCard><GlassCard className="mt-5 p-7"><h2 className="text-xl font-black">How scoring works</h2><p className="mt-2 text-sm text-slate-500">Each dimension remains between 0 and 100. The weighted result is recalculated after every agent cycle.</p><div className="mt-5 grid gap-5 md:grid-cols-5">{[["Signal Accuracy","35% · outcome alignment"],["Confidence Calibration","20% · confidence quality"],["Reaction Speed","15% · early detection"],["Consistency","15% · stable performance"],["Volatility Awareness","15% · instability detection"]].map(x=><div key={x[0]}><StatusBadge tone="blue">{x[0]}</StatusBadge><p className="mt-3 text-xs text-slate-500">{x[1]}</p></div>)}</div></GlassCard></div>}
