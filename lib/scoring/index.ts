import {clamp} from "@/lib/probability";
export function weightedScore(x:{accuracy:number;confidenceCalibration:number;reactionSpeed:number;consistency:number;volatilityAwareness:number}){return clamp(x.accuracy*.35+x.confidenceCalibration*.2+x.reactionSpeed*.15+x.consistency*.15+x.volatilityAwareness*.15)}
export function evolveScore(current:{accuracy:number;confidenceCalibration:number;reactionSpeed:number;consistency:number;volatilityAwareness:number},signals:number,cycle:number){const bump=Math.min(1.8,signals*.55+.15);const metrics={accuracy:clamp(current.accuracy+bump*(cycle%2?1:.45)),confidenceCalibration:clamp(current.confidenceCalibration+bump*.65),reactionSpeed:clamp(current.reactionSpeed+bump*.8),consistency:clamp(current.consistency+bump*.35),volatilityAwareness:clamp(current.volatilityAwareness+bump*.7)};return{...metrics,score:weightedScore(metrics)}}
